<!DOCTYPE html>
<html>
  <head>
    <title>Show Book</title>
  </head>

  <body>

      <h2 class = "center"><b><%= @book.name %></b></h2>
      </br>
      <div class ="blacktext well col-xs-8 col-xs-offset-2 center">
        <h4 class = "center"><strong>Author:</strong></h4>
        <hr> <%= @author.find(@book.author_id).name %>
        <h4 class = "center"><strong>Description:</strong></h4>
        <hr> <%= simple_format(@book.description) %>
        <div class="article-actions">
          <%= link_to "Edit Book", edit_book_path(@book), class: "btn btn-xs btn-primary" %>

          <%= link_to "Back",  books_path ,class: "btn btn-xs btn-success" %>
        </div>
      </div>
  </body>
</html>
